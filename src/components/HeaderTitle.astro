---
interface Props{
    subTitle?: string;
    title: string;
}


const { subTitle, title } = Astro.props;
---
<div>
    {subTitle && <h3 class="text-primary-300">{subTitle}</h3>}
    <h2 class="text-2xl font-bold text-black">{title}</h2>
</div>